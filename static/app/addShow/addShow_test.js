'use strict';

describe('myApp.addShow module', function() {

  beforeEach(module('myApp.addShow'));

  describe('addShow controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});